services:
  - type: web
    name: skillsxai-meet-socket
    runtime: node
    rootDir: socket-server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
